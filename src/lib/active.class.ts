// Abstract.
import { BooleanState } from "./boolean-state.abstract";
/**
 * @description Manages the active state as a `boolean` type .
 * @export
 * @class Active
 * @typedef {Active}
 * @extends {BooleanState}
 */
export class Active extends BooleanState {}