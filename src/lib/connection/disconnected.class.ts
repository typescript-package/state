// Abstract.
import { BooleanState } from "../boolean-state.abstract";
/**
 * @description Manages the disconnected state of `boolean` type.
 * @export
 * @class Disconnected
 * @typedef {Disconnected}
 * @extends {BooleanState}
 */
export class Disconnected extends BooleanState {}
