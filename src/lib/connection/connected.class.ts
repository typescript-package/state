// Abstract.
import { BooleanState } from "../boolean-state.abstract";
/**
 * @description Manages the connected state of `boolean` type.
 * @export
 * @class Connected
 * @typedef {Connected}
 * @extends {BooleanState}
 */
export class Connected extends BooleanState {}
